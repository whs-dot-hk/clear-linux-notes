load("@rules_foreign_cc//tools/build_defs:configure.bzl", "configure_make")

filegroup(
    name = "all",
    srcs = glob(["**"]),
)

configure_make(
    name = "binary",
    lib_source = ":all",
    binaries = [
        "sshpass",
    ],
    visibility = ["//visibility:public"],
)
